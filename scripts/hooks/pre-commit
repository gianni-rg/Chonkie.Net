#!/bin/bash
# Pre-commit hook wrapper for Windows/Linux/macOS
# Install with: git config core.hooksPath scripts/hooks

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
pwsh "$SCRIPT_DIR/validate-headers.ps1" "$@"
